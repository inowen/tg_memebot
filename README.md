# Memebot
Telegram bot that downloads memes (or really any kind of images) from Reddit and sends them to users via Telegram at request.

Requires an installation of python3 and the libraries praw and python-telegram-bot to be run. 

Needs a bot token from telegram and account credentials from reddit to work, those have to be filled into the .ini files.
